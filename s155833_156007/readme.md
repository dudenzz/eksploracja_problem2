Do działania należy zastąpić ścieżki w plikach config.py i config.h ścieżką do zbioru danych.

W pliku makefile znajdują się dwa cele: jeżeli będą problemy z kompilacją `make` proszę spróbować `make python_module2`


